#!/bin/bash
PROXY_URL=$(cat ~/dotfiles/.proxy_url-5.secret 2>/dev/null)

if [ -z "$PROXY_URL" ]; then
  echo "Error: ~/dotfiles/.proxy_url-5.secret not found or empty" >&2
  exit 1
fi

export HTTPS_PROXY="$PROXY_URL"
export HTTP_PROXY="$PROXY_URL"

if [ $# -gt 0 ]; then
  exec "$@"
fi
