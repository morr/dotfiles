#!/usr/bin/env ruby

files = %w(
  .bash_profile
  .gemrc
  .gitconfig
  .pryrc
  .zshrc
)

files.each do |file|
  puts "symlinking #{file}"
  %x(rm -f $HOME/#{file})
  %x(ln ./#{file} $HOME/#{file})
end

puts "done!"
